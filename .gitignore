# Ignore default names for colcon created folders
build
install
log

# Ignore everything in src except a .gitkeep file
src/*
!src/.gitkeep

# pixi environments
.pixi
*.egg-info

# bv (beads viewer) local config and caches
.bv/

# Local environment configuration (user-specific, not committed)
.env
